[build]
  command = "bun run build"
  publish = ".next"

[build.environment]
  BUN_VERSION = "1.4.2"
  BUN_FLAGS = "--frozen-lockfile"
  NODE_VERSION = "22"

# Netlify automatically supplies the Next.js adapter.
# Add secrets in the Netlify dashboard, with Builds and Functions scopes.
